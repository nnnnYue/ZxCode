{
  "name": "video2code",
  "version": "0.6.0",
  "description": "基于 ZCode 内置 Browser Use WebView 录制 WebM 并用 ffmpeg 转成 MP4/URL 复刻；无需 Playwright 或外部 Chromium",
  "author": {
    "name": "Z.ai",
    "url": "https://z.ai"
  },
  "license": "MIT",
  "keywords": [
    "video2code",
    "replication",
    "webapp",
    "motion"
  ],
  "userConfig": {
    "media_resolution": {
      "title": "抽帧分辨率档位",
      "description": "clip_video 抽帧 inline 给模型的每帧分辨率档位: low(~70tok/帧) / medium(~256) / high(~786)",
      "type": "string",
      "default": "medium"
    },
    "clip_max_frames": {
      "title": "单次 clip 帧数上限",
      "description": "一次 clip_video 调用所有 segment 累计抽帧上限 (超出按比例降帧)",
      "type": "number",
      "default": 400
    }
  },
  "description_i18n": {
    "en": "Records WebM through ZCode's built-in Browser Use WebView and converts it to MP4 with ffmpeg for video/URL replication; no Playwright or external Chromium required.",
    "zh-CN": "基于 ZCode 内置 Browser Use WebView 录制 WebM 并用 ffmpeg 转成 MP4/URL 复刻；无需 Playwright 或外部 Chromium"
  },
  "mcpServers": {
    "runtime": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/runtime_server.py"
      ],
      "cwd": "${CLAUDE_PROJECT_DIR}",
      "env": {
        "V2C_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "V2C_MEDIA_RESOLUTION": "${user_config.media_resolution}"
      },
      "timeoutMs": 600000
    },
    "video": {
      "type": "stdio",
      "command": "python3",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/video_server.py"
      ],
      "cwd": "${CLAUDE_PROJECT_DIR}",
      "env": {
        "V2C_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "V2C_MEDIA_RESOLUTION": "${user_config.media_resolution}",
        "V2C_CLIP_MAX_FRAMES": "${user_config.clip_max_frames}"
      },
      "timeoutMs": 300000
    }
  }
}
