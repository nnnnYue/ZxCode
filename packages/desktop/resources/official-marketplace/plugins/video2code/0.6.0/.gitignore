__pycache__/
*.py[cod]

# Per-machine runtime state. .v2c/env_report.json records the absolute
# interpreter and project paths of whoever ran the env doctor (i.e. a
# username), so it must never ship with the plugin or reach the remote.
.v2c/

# Replication run outputs
out/
app/
recordings/
node_modules/
